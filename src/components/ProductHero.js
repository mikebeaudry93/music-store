import React from "react";

const ProductHero = ({ title }) => {
  return (
    <div className="product-hero">
      <h2 className="section-title hero-title">{title}</h2>
    </div>
  );
};

export default ProductHero;
